# Sermon Notes

Place sermon notes PDFs here. The filename must match the `notesPdf` field in
`src/data/church.ts` for each sermon (e.g. `light-of-the-world.pdf`).

These files are served directly from the site root, so the public URL is
`/sermon-notes/<filename>.pdf`.
